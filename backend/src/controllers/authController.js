// Placeholder controller
exports.register = (req, res) => {
  res.json({ message: 'Register endpoint - work in progress' });
};

exports.login = (req, res) => {
  res.json({ message: 'Login endpoint - work in progress' });
};